using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SwipeScrollRect : MonoBehaviour
{
    [SerializeField]
    private ScrollRect _scrollRect;


    private void Update()
    {
        if(!Input.GetMouseButton(0))
        {
            //ScrollViewFocusFunctions.FocusOnItemCoroutine(
        }
    }
}